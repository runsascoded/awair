{
  "name": "awair-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that monitors Awair device freshness and sends Pushover alerts when devices go stale.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.80.0"
  }
}
