# SPA fallback: any unmatched path renders `index.html` with a 200 (edge
# rewrite, no redirect hop, no `Location:` header). Client-side JS reads
# `location.pathname` to route between the chart (`/`), the health
# dashboard (`/health`), and the shard browser (`/files/*`).
/*  /index.html  200
